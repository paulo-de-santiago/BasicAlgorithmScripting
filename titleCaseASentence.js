function titleCase(str) {
  //get and array of the words of the sentence
  let words = str.split(" ");
  console.log(str);
  console.log(words);
  console.log(words.length);

  for (let i = 0; i <= words.length; i++) {
    //get the first
    console.log;
    for (let j = 0; j < words[i].length; j++) return words;
  }

  return str;
}

console.log(titleCase("I'm a little tea pot"));
